# Memory data directory

This directory stores persistent data for the four-layer memory system:
- Episodic memory (JSON files)
- Semantic memory (rules + best practices)
- Procedural memory (usage patterns)
